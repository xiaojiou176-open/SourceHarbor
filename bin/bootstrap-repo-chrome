#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
# shellcheck source=./scripts/runtime/entrypoint.sh
source "$ROOT_DIR/scripts/runtime/entrypoint.sh"
sourceharbor_entrypoint_bootstrap runtime bootstrap-repo-chrome "" "$@"
exec python3 "$ROOT_DIR/scripts/runtime/bootstrap_repo_chrome.py" "$@"
